import { NAMESPACE } from "../../shared/const";

export const WEBVIEW_CARD_TYPE = `${NAMESPACE}-webview-card`;
export const WEBVIEW_CARD_NAME = "Ted Web View Card";
export const WEBVIEW_CARD_DESCRIPTION =
  "A full-bleed web view (iframe) whose URL comes from config or a `?url=` navigation parameter.";
